server:
  port: 8081

spring:
  application:
    nane: fraud
  datasource:
    username: amigoscode
    url: jdbc:postgresql://localhost:5433/fraud
    password: password
    jpa:
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
          show_sql: 'true'
      hibernate:
        ddl-auto: update
        show_sql: 'true'